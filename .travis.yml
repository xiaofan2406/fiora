sudo: false
language: node_js
cache:
  directories:
    - node_modules
node_js:
  - "8"
before_script:
  - rm -rf ./coverage ./lib
install:
  - yarn global add codecov
  - yarn
script:
  - yarn build
  - yarn coverage
  - codecov
