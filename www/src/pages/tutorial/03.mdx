
#### 2. Add a simple username field

Now lets add a username field.

```jsx
<Form>
  <Field name="username">
    {({ value, handleChange }) =>
      <input
        type="text"
        onChange={({ target: { value } }) => handleChange(value)}
      />
    }
  </Field>
</Form>
```
